#include<stdio.h>
 intmain()
{
	int rows,n,column;
	scanf("enter a value for n",&n);
	for(row=1;rows<=n;rows++)
	{
		for(column=1;column<=rows;column++)
		printf("*");
		
	}
	printf(" ");
}
